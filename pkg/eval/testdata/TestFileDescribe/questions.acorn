args: {
    // a description
    // some condidionts
    a_number_with_a_default: number > 3 && number < 10
    // this is what an enum looks like
    a_enum_yeah_weird?: enum("one", "two", "three")
    // this is a custom condition which you have to just throw your hands up with
    a_custom_condition?: int
    // a description
    a_bool_with_default: false
    // a description
    a_string_with_default: "hi"
    // an array of strings
    an_array_of_strings: [string]
    an_array_of_numbers: [number]
    // This is an array
    an_array_of_objects: [{
        match "hi": {
            foo: "bar"
        }
    }]
    // This is an object
    an_object: {
        // This field is required
        required_field:"hi"
        // This field is optional
        optional_field?:"hi"
        // This field matches a regexp
        match "some regexp": 2
    }
}

